{
  "name": "ecbc-truck-reservations",
  "version": "1.0.0",
  "private": true,
  "description": "Static Web App for ECBC Truck reservations and check-in/check-out calendar.",
  "scripts": {
    "start": "swa start frontend --api-location api",
    "deploy": "swa deploy"
  },
  "devDependencies": {
    "@azure/static-web-apps-cli": "^1.1.10"
  }
}
